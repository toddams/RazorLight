using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace RazorLight
{
    public class Class
    {
	    public void Go()
	    {
			/*
		    var config = PageBuilder
			    .Init()
			    .UseFileSystemTemplates()
			    .AddLayoutPage("/Shared/_Layout")
			    .AddViewImports("/Shared/ViewImports")
			    .Cache(true)
			    .SetSlidingExpiration(TimeSpan.FromMinutes(20))
			    .Build();

		    var template = engine.GetPage(config);
			*/
	    }
    }
}
